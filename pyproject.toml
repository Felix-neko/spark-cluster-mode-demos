[project]
name = "spark-cluster-mode-demos"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.7"
dependencies = [
    "more-itertools>=9.1.0",
    "pyarrow<=8.0.0",
    "pyspark<4",
]
